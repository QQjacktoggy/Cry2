[project]
name = "jackbot"
version = "1.0.0"
description = "Jackbot_V1 — Binance Futures Grid Day-Trading Bot"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pydantic>=2.13.3",
    "structlog>=25.5.0",
]
